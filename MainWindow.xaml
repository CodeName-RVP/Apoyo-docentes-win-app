<Window x:Class="AppParaUniversidad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:AppParaUniversidad.Views"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Apoyo Docentes" Height="780" Width="1200" MinHeight="650" MinWidth="1000"
        WindowStyle="None" ResizeMode="CanResize" Background="{DynamicResource WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" CornerRadius="0" GlassFrameThickness="0" />
    </shell:WindowChrome.WindowChrome>
    <Border BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{DynamicResource TitleBarBrush}" MouseLeftButtonDown="OnTitleBarMouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Cargas Docentes" Margin="10,0,0,0" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TabControl x:Name="MainTabs" TabStripPlacement="Top" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="0,4,6,4">
                        <TabControl.Template>
                            <ControlTemplate TargetType="TabControl">
                                <TabPanel IsItemsHost="True" Background="Transparent" />
                            </ControlTemplate>
                        </TabControl.Template>
                        <TabControl.ItemContainerStyle>
                            <Style TargetType="TabItem">
                                <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}" />
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentDarkBrush}" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="MinHeight" Value="32" />
                                <Setter Property="Padding" Value="12,4" />
                                <Setter Property="Margin" Value="0,0,8,0" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                            <Border x:Name="Bd"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"
                                                                  ContentSource="Header" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ButtonHoverBrush}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource ButtonHoverForegroundBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentDarkBrush}" />
                                                    <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Opacity" Value="0.6" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TabControl.ItemContainerStyle>
                        <TabItem Header="Cargas">
                            <views:LoadsView x:Name="LoadsViewControl" />
                        </TabItem>
                        <TabItem Header="Envio">
                            <views:SendView x:Name="SendViewControl" />
                        </TabItem>
                        <TabItem Header="Directorio">
                            <views:DirectoryView x:Name="DirectoryViewControl" />
                        </TabItem>
                        <TabItem Header="Importar / Exportar">
                            <views:ImportExportView x:Name="ImportExportViewControl" />
                        </TabItem>
                        <TabItem Header="Configuracion">
                            <views:SettingsView x:Name="SettingsViewControl" />
                        </TabItem>
                    </TabControl>
                    <Button Style="{StaticResource TitleBarButtonStyle}" Content="{DynamicResource IconMinimize}" Width="40" Click="OnMinimizeClick" />
                    <Button Style="{StaticResource TitleBarMaximizeButtonStyle}" Width="40" Click="OnMaximizeClick" />
                    <Button Style="{StaticResource TitleBarCloseButtonStyle}" Content="{DynamicResource IconClose}" Width="40" Click="OnCloseClick" />
                </StackPanel>
            </Grid>

            <ContentPresenter Grid.Row="1" Content="{Binding ElementName=MainTabs, Path=SelectedContent}" />
        </Grid>
    </Border>
</Window>






