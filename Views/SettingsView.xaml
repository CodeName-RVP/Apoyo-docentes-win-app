<UserControl x:Class="AppParaUniversidad.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource CardBrush}" Padding="10" CornerRadius="8" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Cuenta Gmail" FontWeight="SemiBold" Margin="0,0,0,6" />
                <Button Content="{Binding GmailButtonText}"
                        Command="{Binding LoadCredentialCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding GmailReady}" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource SuccessBrush}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding GmailReady}" Value="False">
                                    <Setter Property="Background" Value="{DynamicResource DangerBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <TextBlock Text="{Binding GmailStatus}" Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource CardBrush}" Padding="10" CornerRadius="8" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1" Margin="0,8,0,0">
            <StackPanel>
                <TextBlock Text="Tamano de ventana al iniciar" FontWeight="SemiBold" Margin="0,0,0,6" />
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Tamano:" VerticalAlignment="Center" Margin="0,0,6,0" />
                    <ComboBox ItemsSource="{Binding WindowSizePresets}"
                              SelectedItem="{Binding SelectedWindowSize}"
                              DisplayMemberPath="Label"
                              Width="220"
                              Margin="0,0,12,0" />
                    <Button Content="Aplicar tamano" Command="{Binding ApplySizeCommand}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="{DynamicResource CardBrush}" Padding="10" CornerRadius="8" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tema oscuro" VerticalAlignment="Center" Margin="0,0,8,0" />
                <CheckBox IsChecked="{Binding DarkTheme}" />
                <Button Content="Aplicar tema" Command="{Binding ToggleThemeCommand}" Margin="8,0,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="3" Background="{DynamicResource CardBrush}" Padding="10" CornerRadius="8" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="{Binding UpdateButtonText}" Command="{Binding CheckUpdatesCommand}" IsEnabled="{Binding UpdateButtonEnabled}" />
                <TextBlock Text="{Binding UpdateStatusText}" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding UpdateAvailable}" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding UpdateCheckSucceeded}" Value="True" />
                                        <Condition Binding="{Binding UpdateAvailable}" Value="False" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}" />
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
