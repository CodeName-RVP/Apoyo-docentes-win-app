<UserControl x:Class="AppParaUniversidad.Views.ImportExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource CardBrush}" Padding="10" CornerRadius="8" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1">
            <WrapPanel>
                <Button Content="Exportar JSON" Command="{Binding ExportCommand}" />
                <Button Content="Importar (merge)" Command="{Binding ImportMergeCommand}" />
            </WrapPanel>
        </Border>

        <TextBlock Grid.Row="1" Text="{Binding Status}" FontWeight="SemiBold" Margin="0,8,0,8" />

        <Border Grid.Row="2" Background="{DynamicResource CardBrush}" Padding="6" CornerRadius="8" BorderBrush="{DynamicResource BorderBrushLight}" BorderThickness="1">
            <DataGrid ItemsSource="{Binding PreviewContacts}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding NombreVisible}" Header="Nombre" />
                    <DataGridTextColumn Binding="{Binding Correo}" Header="Correo" />
                    <DataGridTextColumn Binding="{Binding Telefono}" Header="Telefono" />
                    <DataGridCheckBoxColumn Binding="{Binding Activo}" Header="Activo" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
